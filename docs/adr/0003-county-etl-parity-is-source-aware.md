# County ETL parity is source-aware

County ETL parity means equivalent safe tax-protest outcomes and operational
controls, not identical schemas, fields, or source coverage. A county may map
only source-verified semantics; an unavailable or non-equivalent field remains
explicitly unavailable with provenance instead of being inferred or presented
as a certified current-year value.

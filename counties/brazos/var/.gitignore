# Runtime data for this county's ETL: downloaded archives, extracted source
# files, logs, and generated reports. All regenerated by the import commands;
# none of it belongs in version control. This file keeps the directory present.
*
!.gitignore
